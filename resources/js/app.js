import './bootstrap';
import 'flowbite';


// If you want Vue to be inline in your Blade files, you don’t need to import components here.
// Instead, we will mount it inline in the Blade template.

// Vue({}).mount('#app');